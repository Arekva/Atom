using Atom.Engine.Astro;

namespace Atom.Engine.Generator;

public class SphereGenerator : IGenerator
{
    public void Generate(u128 location, u32 depth, Span<Voxel> voxels)
    {
        for (u32 i = 0; i < Cell.UNITS; i++)
        {
            
        }
    }
}